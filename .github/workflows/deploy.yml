name: Build and Deploy to cPanel

on:
  push:
    branches:
      - master # Your branch name (change if needed)

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Install Dependencies
        run: npm install

      - name: Build Project
        run: npm run build

      - name: Upload via FTP to cPanel
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: "103.86.176.168"
          username: "ram@testing.vmvanigam.com"
          password: ${{ secrets.CPANEL_PASSWORD }}
          protocol: ftp # change to ftp if server doesn't support FTPS
          port: 21
          local-dir: ./dist/ # vite build output
          server-dir: /testing/ # destination folder on cPanel
