#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

echo "🚀 Running pre-commit checks..."

# Run lint-staged
npx lint-staged || exit 1

echo "🔎 Checking for merge conflict markers..."

# # Merge conflict detection (ignore Husky, node_modules, dist)
# CONFLICTS=$(grep -rn "<<<<<<<" . --exclude-dir=.husky --exclude-dir=node_modules --exclude-dir=dist)

# if [ -n "$CONFLICTS" ]; then
#   echo "❌ Merge conflicts found at:"
#   echo "$CONFLICTS"
#   exit 1
# fi

echo "✔ Pre-commit checks passed!"
